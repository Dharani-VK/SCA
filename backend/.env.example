# AI API Keys (Leave blank for Demo Mode / Local Fallback)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

GROQ_API_KEY=
GROQ_MODEL=mixtral-8x7b-32768

# Security
APP_SECRET_KEY=change_me_in_prod
# DATABASE_URL=sqlite:///analytics.db # Default is sufficient for local dev

# Demo Mode (Set to true to force fallback behavior even if keys are present)
IS_DEMO_MODE=false
